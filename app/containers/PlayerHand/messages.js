/*
 * PlayerHand Messages
 *
 * This contains all the text for the PlayerHand container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PlayerHand';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PlayerHand container!'
  }
});
